# CLAUDE.md - 贪吃蛇游戏项目

## 项目概述

- **项目名称**: tanchishe-snake-game（贪吃蛇）
- **目标**: 一个可部署的贪吃蛇游戏网站，用户打开浏览器即可游玩
- **仓库**: https://github.com/Cayre1021/tanchishe-snake-game

## 技术栈

- **纯前端**: HTML + CSS + JavaScript，无框架、无构建工具
- **画布渲染**: 使用 `<canvas>` 绘制游戏画面
- **部署**: 静态文件，可直接通过 GitHub Pages 托管
- **不引入任何第三方依赖或 CDN**

## 项目结构

```
tanchishe/
├── index.html        # 入口页面
├── css/
│   └── style.css     # 样式
├── js/
│   └── game.js       # 游戏全部逻辑（蛇、食物、输入、主循环）
├── CLAUDE.md         # 项目规范（本文件）
└── README.md         # 项目说明
```

## 核心功能

1. **游戏基础**
   - 蛇在网格上移动，方向键控制上下左右
   - 吃到食物后蛇身增长，分数增加
   - 撞墙或撞到自身则游戏结束
   - 游戏结束后显示分数，支持重新开始

2. **UI/UX**
   - 开始界面：显示游戏标题和"开始游戏"按钮
   - 游戏界面：画布 + 实时分数显示
   - 结束界面：显示最终分数 + 重新开始按钮
   - 支持移动端触屏滑动控制
   - 响应式布局，适配不同屏幕尺寸

3. **视觉风格**
   - 像素风格 / 简洁现代风格均可
   - 蛇身与食物颜色区分明显
   - 网格背景辅助视觉定位

## 代码规范

- 使用 ES6+ 语法，模块化组织代码
- 常量统一定义在文件顶部（网格大小、速度、颜色等）
- 游戏逻辑与渲染分离
- 不使用 `var`，统一用 `const` / `let`
- 函数命名用 camelCase，常量用 UPPER_SNAKE_CASE

## 禁止改动

- **不要引入任何 npm 包、CDN 或外部依赖**
- **不要使用任何 JS 框架（React/Vue/Angular 等）**
- **不要修改本文件（CLAUDE.md）的核心规范，除非用户明确要求**
- **不要添加后端逻辑，纯前端项目**
- **不要使用 TypeScript，保持纯 JavaScript**

## 验收标准

- [ ] 浏览器打开 `index.html` 可直接运行，无需任何构建步骤
- [ ] 键盘方向键可控制蛇的移动
- [ ] 吃食物后蛇增长、分数增加
- [ ] 撞墙或撞自身时游戏结束
- [ ] 支持重新开始游戏
- [ ] 移动端可触屏操作
- [ ] 页面响应式，手机和电脑都能正常显示
